// Marker class so Blazor's Router can locate the assembly via typeof(Program).Assembly.
// The actual host entrypoint lives in the STS project (sufficit-identity/src/sts/Program.cs).
namespace Sufficit.Identity.UI;

internal static class Program { }
